import { createContext } from "react";
import React from "react";
import { List } from "../components/list/Liist";
import { SendValue } from "./send";
// Create the context


export const Chat = () => {
  const message = 'text me now'
  return (
    <div>
      <div style={{display:'none'}} >
      </div>
    </div>
    
  );
};
